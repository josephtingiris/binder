; https://www.ietf.org/rfc/rfc1912.txt
$ORIGIN ++ORIGIN++.
$TTL 1H ; default time to live
@ IN SOA ++MNAME++. ++RNAME++. (
        ++SERIAL++ ; serial (do NOT use date based values)
        20M        ; refresh
        5M         ; retry
        3W         ; expire
        1H         ; minimum
        )
@ NS    ++NS++.
